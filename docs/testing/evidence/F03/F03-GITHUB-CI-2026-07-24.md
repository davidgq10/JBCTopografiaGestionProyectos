# Evidencia GitHub Actions y protección de `Main`

Fecha: **2026-07-24**.

## Destino

- repositorio: [davidgq10/JBCTopografiaGestionProyectos](https://github.com/davidgq10/JBCTopografiaGestionProyectos);
- rama predeterminada: `Main`;
- commit inicial publicado: `a93d0dac1eedced649d0eb5d7041adb52fa23f23`;
- pull request de cierre: [PR #1](https://github.com/davidgq10/JBCTopografiaGestionProyectos/pull/1);
- ejecución CI del corte conectado: [GitHub Actions #30117552690](https://github.com/davidgq10/JBCTopografiaGestionProyectos/actions/runs/30117552690);
- revisión de dependencias: [GitHub Actions #30117552696](https://github.com/davidgq10/JBCTopografiaGestionProyectos/actions/runs/30117552696).

## Resultado observado

- workflow `CI`: **PASS**;
- job `Quality`: **PASS**, con Node.js 24, pnpm 11.9 y Chromium;
- job `Database`: **PASS**, con PostgreSQL 17 efímero, migraciones, estructura, RLS y walking skeleton;
- job `Connected integration`: **PASS**, con Chromium, PostgREST 14.12, PostgreSQL 17, RLS y auditoría;
- job `Dependency review`: **PASS**, con 0 paquetes vulnerables y 0 licencias incompatibles;
- no se requirieron secretos de aplicación, Entra ni producción.

## Protección aplicada

La rama `Main` exige pull request, una aprobación, descarta aprobaciones obsoletas, exige rama actualizada, conversaciones resueltas, historial lineal y los cuatro checks `Quality`, `Database`, `Connected integration` y `Dependency review`. La regla también se aplica a administradores; force-push, eliminación y bypass ordinario permanecen deshabilitados.

`Dependency review` está configurado para pull requests y bloquea severidad alta o crítica. El gráfico de dependencias se habilitó en el repositorio y la reejecución pasó con 0 paquetes vulnerables y 0 licencias incompatibles. El workflow usa `actions/dependency-review-action@v5` para mantener compatibilidad con Node 24.

Los cuatro checks quedaron registrados, ejecutados correctamente y configurados como requeridos en la regla de protección de `Main`.

Los avisos de runtime Node.js 20 observados en la primera ejecución motivaron la actualización a `actions/checkout@v7`, `actions/setup-node@v7` y `pnpm/action-setup@v6`, que se verifica mediante pull request antes de integrar.
