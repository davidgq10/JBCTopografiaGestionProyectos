# Preparación de Microsoft Entra QA

Fecha: **2026-07-28 America/Costa_Rica**.

## Comprobaciones no interactivas

- Supabase Auth local respondió `200` en `/auth/v1/settings` y reportó `external.azure=true`.
- El descubrimiento OIDC del tenant QA respondió `200` desde `https://login.microsoftonline.com/<tenant-qa>/v2.0/.well-known/openid-configuration`.
- El callback configurado es `http://localhost:54321/auth/v1/callback`.
- La primera ejecución dejó `env(...)` literal porque la sustitución de Supabase CLI requiere un `.env` en el directorio de trabajo; se corrigió reiniciando desde un directorio temporal fuera del repositorio que contiene ese `.env` y conserva el secreto fuera de OneDrive.
- Después de la corrección, la URL authorize generada usa el tenant QA y el client ID real; no contiene marcadores `env(...)`.
- Las variables de cliente/URL/secreto permanecen fuera del repositorio; esta evidencia no contiene secretos.

## Resultado de la prueba real

**PASS — 2026-07-28 America/Costa_Rica.** En el perfil Chrome autorizado se completó el flujo Microsoft Entra del tenant QA con MFA y se volvió a la aplicación local sin tocar producción.

- La autorización utilizó el client ID QA y el callback exacto `http://localhost:54321/auth/v1/callback`.
- El ajuste local definitivo es `[api] external_url = "http://localhost:54321"`; el puerto temporal de la UI fue `http://localhost:4174` para iniciar con almacenamiento limpio del navegador.
- La pantalla mostró **“Sesión autorizada”**, el perfil de la organización y el rol `technician` después de validar `app_users` y `user_roles` mediante RLS.
- Se ejecutó revocación controlada en QA (`is_active=false`, motivo y fecha), la UI mostró **“Su cuenta de Microsoft no está autorizada o fue revocada”**, y luego se restauró `is_active=true` sin eliminar la identidad.
- Después del alta inicial, `GOTRUE_DISABLE_SIGNUP=true`; no se conservaron secretos en el repositorio ni en esta evidencia.

El primer intento con `127.0.0.1` produjo el rechazo esperado de Azure para plataformas Web (`AADSTS50011`). Se resolvió usando `localhost`, conforme a la configuración de Supabase para Azure local; la URI `127.0.0.1` no se añadió al registro de producción.
